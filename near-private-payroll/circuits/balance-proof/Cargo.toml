[package]
name = "balance-proof"
version = "0.1.0"
edition = "2021"

[dependencies]
risc0-zkvm = { version = "1.0", default-features = false, features = ["std"] }
serde = { version = "1.0", features = ["derive"] }
sha2 = "0.10"

[[bin]]
name = "balance-proof"
path = "src/main.rs"
